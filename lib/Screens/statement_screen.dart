import 'package:flutter/material.dart';

class StatementPage extends StatelessWidget {
  static final routeName = "/statement";
  const StatementPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Hello world!"),
      ),
    );
  }
}
